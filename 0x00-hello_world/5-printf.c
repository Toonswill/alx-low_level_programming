#include <stdio.h>

/**
 * main - Outputs a string with proper grammar
 *
 * Return: 0 to exit the program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
